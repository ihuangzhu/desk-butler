<Window x:Class="DeskButler.Desktop.Views.RecoveryCardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="恢复上次现场" Width="420" SizeToContent="Height" MaxHeight="620"
        WindowStartupLocation="CenterScreen" WindowStyle="ToolWindow" ResizeMode="NoResize"
        Topmost="True" ShowActivated="False" ShowInTaskbar="False" Background="#FFFDFDFD">
    <Border Padding="20" BorderBrush="#FFD4D4D8" BorderThickness="1" CornerRadius="10">
        <StackPanel>
            <TextBlock Text="回到上次工作现场？" FontSize="20" FontWeight="SemiBold" />
            <TextBlock Margin="0,8,0,14" Foreground="#FF66666F" TextWrapping="Wrap"
                       Text="请选择要恢复的窗口。15 秒后卡片只会隐藏，不会自动执行任何操作。" />
            <ScrollViewer MaxHeight="300" VerticalScrollBarVisibility="Auto" Focusable="True">
                <ItemsControl ItemsSource="{Binding Items}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <DockPanel Margin="0,0,0,8">
                                <Button DockPanel.Dock="Right" Content="永久排除" Padding="8,3" Margin="8,0,0,0"
                                        Command="{Binding DataContext.ExcludePermanentlyCommand, RelativeSource={RelativeSource AncestorType=Window}}"
                                        CommandParameter="{Binding}" />
                                <CheckBox IsChecked="{Binding IsSelected}" Content="{Binding DisplayName}"
                                          VerticalAlignment="Center" Focusable="True" />
                            </DockPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
            <TextBlock Text="{Binding ErrorMessage}" Foreground="#FFB42318" TextWrapping="Wrap"
                       Margin="0,8,0,0" AutomationProperties.LiveSetting="Assertive" />
            <WrapPanel Margin="0,12,0,0" HorizontalAlignment="Right">
                <Button x:Name="FirstActionButton" Content="_立即恢复" Command="{Binding RestoreImmediatelyCommand}" Padding="12,6" Margin="0,0,8,8" />
                <Button Content="_安全恢复" Command="{Binding RestoreSafelyCommand}" Padding="12,6" Margin="0,0,8,8" />
                <Button Content="_跳过" Command="{Binding SkipCommand}" Padding="12,6" Margin="0,0,0,8" />
            </WrapPanel>
        </StackPanel>
    </Border>
</Window>
